# 装着ギャラリー改修

- ユーザー指定の新TOP案 `http://127.0.0.1:4177/preview.html?task=01a01cbb-59df-7d80-816c-9ec489c2632a` をデザイン基準にする。実体は prototype の4173プレビュー。
- 最新のユーザー指定（2026-09-14）は「一個まえがよかった」。現行風への改修を取り消し、その直前の新TOPに合わせた動画版へ戻した。左寄せのBebas Neue見出し、白背景、ヒーロー直下の検索を基準とする。
- その後の最新依頼「現デザインのヒーロー動画に」により、動画だけ現行ギャラリーの https://seatcover.jp/gallerys/v2/alphard.mp4 へ変更。左寄せの見出し・白背景・検索レイアウトを維持。動画URLはpublic/index.html。取り消した中央セリフ体・クリーム色・縦長注目写真の構成へ戻さない。
- 最新依頼「もうちょっとヒーローおおきくてもいい」で動画エリアの高さを拡大。PCは590〜660px、スマホは560〜620px（360px以下は520px）。見出しは左寄せのまま縦方向中央に配置。public/hero-video.cssで制御。
- 最新の検索追加: old.zipを調査し、ブランド・シリーズ検索を確認。ZIP内には色専用欄も色項目もない。別途保存HTMLの写真説明（alt）に残る52色名・2,706件を使い、色系統と掲載色名の複合検索を追加。写真やレビューから色を推測しない。IXUS143件をZIPから追加し合計2,897件。IXUSは車種名・色名未掲載として扱い、車種件数に含めない。ブランド・シリーズ・色はスマホでも常時表示。
- 最新依頼「表示が小さい・直感的に押せない」への改善: discovery-filters.cssでブランドを日本語名付きの大きな枠ボタン、色を色面・名称・件数付きの大きな枠ボタンへ変更。選択時は赤枠＋チェック。ブランドPC4列、スマホ2列。色PC7列、スマホ4列（360px以下3列）で横スクロールしない。検索・車種・シリーズの文字と操作領域も拡大。小さい文字だけのブランド列や直径23pxの色表示へ戻さない。
- TOP本体は別タスクでも作業中。新ギャラリーはこのフォルダ内で制作し、TOP側を勝手に変更しない。
- 最新依頼「ブランドロゴ」: 新TOPの既存SVG4種をpublic/assets/brandsへ同一内容でコピー。ブランドボタンの正式ロゴ・読み・件数・選択チェックを維持。
- 参考 https://sandii.net/gallery/ と「全シリーズ縦画像を代表シリーズイメージとして使って」に対応。公式 https://seatcover.jp/c/seatcovermaker のPICK UPから51枚（576/577×680）の縦画像を取得しpublic/assets/seriesへ無加工保存。出典・対応URL・SHA256はaudit/series-assets.json。シリーズを写真で選ぶカード（PC6列、1400px以下4列、スマホ2列）、初期から全シリーズを表示し、結果への導線を追加。51件は公式掲載シリーズ数であり装着事例数ではない。
- series-data.jsの明示的な別表記対応だけをfilter.jsで集約する。パネル、Heritage Kachina、Dotty LUXUR-BS、IXUSファブリック等を別製品に推測統合しない。代表画像と対応未確認の旧シリーズは「その他の掲載シリーズ」と元の選択欄から検索可能。未収録の新シリーズは代表写真を表示し「装着写真は準備中」。写真から色・適合を補完しない。
- 実際の装着事例と元写真を使う。レビュー、星、色、年式、適合を捏造しない。資料由来の集計値と本番の最新値を区別する。
- 旧版のメーカー・車種名・ブランド・カテゴリ・シリーズ検索を活かし、検索データと写真の段階表示を分離する。
- プレビューは4180番のループバック。scripts/start-preview.ps1で独立起動し、.previewにPID・ログ・起動時刻を記録する。既存の同プロジェクトは再利用。別プロセスのポートを奪わない。返答終了で停止しない。
- 2026-09-15に「コミットPUSHデプロイ」「URL知っている人全員が見れるように」の明示依頼あり。専用SitesとGitはDEPLOY.mdを参照。ログイン不要の公開設定とnoindexを維持。旧HTMLの復元スナップショットはローカル比較専用。本番へ丸ごと戻さない。

- 最新指定「なぜ4つしか出ない？」に対応し、初期4/6件制限と折り畳みボタンを削除。選択ブランドの全シリーズ（Sandii14件）を最初から表示する。再び一部だけ隠さない。

- 最新指定「これはいらない／絞り込んでもらう」でブランドの「すべて」を削除。Refinad・Sandii・Dotty・IXUSの4択。ブランド未選択時はシリーズ写真を表示せず、シリーズ名選択も無効にし、ブランド選択を案内する。選択後はそのブランドの全シリーズを初期表示。リセットやブランド条件解除で未選択に戻り、シリーズ・掲載色名の従属条件も解除する。初期ブランドを勝手に選ばない。

- 最新指定（2026-09-15）「〇ロゴに変更」: ブランド選択を円形のロゴ表示に変更。公式ブランド一覧のカラーJPGを無加工保存し、CSSの円形枠内で全体を表示。PC136px・SP116px（320pxでは104px）、選択は赤い円とチェック。旧SVGの拡大トリミングへ戻さない。出典はaudit/brand-maker-assets.json。
- 同日の「車メーカーの検索も追加」: 車メーカーのロゴ選択をブランドの上へ常設。国産10社／輸入14社を切り替え、地域内の全ロゴを表示。maker-data.jsは収録24メーカーに対応。メーカー・車種のセレクトを共通位置へ移し、SPの追加条件ダイアログ内へ隠さない。メーカー変更・解除では車種も解除し、ブランド・シリーズ・色の条件は維持。メーカーのロゴ素材は新TOPから同一バイト列でコピー、不足3社は同TOP記載の公式URLから取得。

## 最新指定: WEB調査に基づく絞り込みの再構成（2026-09-15）

- 「WEBで絞り込み わかりやすい サイトを調査し」に対応。調査と採用理由はaudit/filter-ux-research-2026-09-15.md。以降の配置はこの節を優先する。
- PC/SP共通で「車で探す」「ブランドで探す」「色で探す」「その他の条件」の大きな入口と切り替えパネル。条件は入口のすぐ下にまとめ、個別解除・全解除を表示。順番を強制しない。
- 車パネルは国産/輸入の全メーカー丸ロゴと、メーカーに連動する車種候補の名前検索・件数付きボタン。小さいネイティブ車種選択へ戻さない。メーカー変更/解除では車種も解除するが、他の条件は保持する。
- ブランドパネル内は4つの丸ロゴと選択ブランドの全シリーズ縦写真を最初から表示。ブランド「すべて」、初期ブランドの自動選択、シリーズの4/6件制限・折り畳みを追加しない。旧シリーズは「その他の掲載シリーズ」のボタンで探せる。重複するシリーズセレクトは削除済み。
- PCサイドバー・SP追加条件ダイアログは廃止。カテゴリとコメント条件は「その他の条件」パネル。長い一覧でも画面下の件数付きボタンから写真へ進める。操作のたびに勝手に写真へスクロールしない。
- 0件時は「この条件を外すと何件」を実データで計算して表示。自動解除しない。車種未掲載の事例はメーカーだけで閲覧できる案内を表示し、推測した車種を足さない。
- 最終の配置CSSはpublic/finder-workbench.css（discovery-filters.cssの後に読み込む）。純粋な条件解除・車種候補・0件回復計算はpublic/finder-state.js。UIはapp.js/index.html。改修前は.source/design-revisions/20260915-before-filter-uxへ退避。

## 最新指定: SP/PCのごちゃつきを減らす（2026-09-15）

- ユーザーの「SP/PCともにごちゃごちゃしてる感ある」に対応。この節を前の配置指定より優先する。
- 入口はPC/SPともに「車種・ブランド・カラー・その他」の1行タブ。大きな枠カード、説明の繰り返し、人気車種の別列、パネル末尾の重複ナビゲーションは廃止。操作面積はタブ55px以上・主要ボタン54px以上を保つ。
- 未選択時の条件ボックスは表示しない。選択時にだけ、値と×のチップ・全解除を表示。メーカー・ブランド・シリーズ等の個別解除はこの行へ統一する。キーワード入力と詳しい色名は明示した操作で開く。
- 「写真を見る」は同一の1ボタン。通常は条件パネル末尾、長い一覧で末尾が画面外なら同じボタンが追従する。以前の固定件数バーと重複する閲覧ボタンへ戻さない。写真一覧では追従を終了する。
- 丸ロゴ、選択ブランドの全シリーズ写真、メーカー・車種・ブランド・シリーズ・色のAND、0件回復を維持。メーカー・ブランド・色の一覧では繰り返す件数を視覚的に省略し、合計は写真ボタンで表示。車種候補とシリーズは件数を表示。
- ブランド選択の黒い四角背景を解除し、赤い円・チェックで示す。CSSは引き続きfinder-workbench.css。改修前は.source/design-revisions/20260915-before-simplifyへ退避。

- 最新の画像指定「ここはもうちょっと小さくしたがいい」（2026-09-15）: ブランドの丸ロゴをPC112px／SP92px（360px以下84px）へ約2割縮小。ブランド一覧幅はPC760px／SP320px、SPの行間10px、ボタン内余白も縮める。ブランドの読み・丸枠・選択チェック、SP2列と全シリーズ写真は維持。メーカーのロゴは対象外。

- 最新指定「英字をJosefin Sansにしようか」（2026-09-15）: 英字・数字をJosefin Sansに変更。Bebasを基準にする以前の書体指定より優先する。日本語のHiragino/Yu Gothic/Meiryoスタックと画像の正式ロゴは維持。public/typography.cssを最後に読み込む。新TOP内の@fontsource/josefin-sans 5.3.0からLatin400/500/600のWOFF2を同一バイトでコピーしOFL同梱、audit/typography-assets.jsonに出典・ハッシュ。ヒーローは左寄せの2行のまま、Josefinの幅に合わせ文字サイズ・字間・コピー幅を調整。動画・高さ・検索の簡潔な構成は維持。
- 続く「ヒーロー英字やや小さく」: INSTALLATION GALLERYだけをSP/PCとも約1割縮小。typography.cssのh1をPC clamp(42px,5vw,76px)、SP clamp(29px,8.8vw,61px)、360px以下clamp(26px,8.8vw,32px)へ変更。Josefin Sans、2行、ヒーローの高さ、日本語コピーは維持。
- 「絞り込みとどこにも書いてない」への対応: タブの直上に小さなフィルターアイコンと「絞り込み」を明記（finder-filter-label）。写真一覧から戻るボタンも「絞り込みを変更」に統一。大きい枠や長い説明は追加せず、既存の簡潔なタブ構成を保つ。
- 「テキストやや小さく」は続く画像と「これの事」によりヒーロー内の日本語が対象。絞り込みUIは変更しない。typography.cssで日本語見出しをPC clamp(16px,1.35vw,21px)／SP15px／360px以下14px、説明文をPC11px／SP10.5pxへ一段階縮小。英字見出し・ボタンのサイズは前回のまま。
- 2026-09-16「絞りこみ検索である事を」: 検索セクションの主見出しを「装着写真を絞り込み検索」へ変更。タブ直上は「条件を選ぶ」。スマホでは「装着写真を／絞り込み検索」のまとまりで折り返す。説明や枠を増やさず、絞り込みの用途を主見出しで示す。
- 同日の「ヘッダーフッターを現状のHPに揃えて」: 共通部分を公開中のseatcover.jpに統一。黒い案内帯、商品ナビ・商品検索・マイカー/会員/お気に入り/カート/ガイド/CLOGの公式SVG、SPのMENUを追加。フッターは現行の7案内項目・会員/規約リンク・SNS・ロゴをPC4列/SP開閉で表示。store-chrome.css/jsで名前を分けて実装し、ギャラリーの絞り込みとは独立させる。ヘッダーロゴは新TOPプレビューでなくseatcover.jpへ。取得元と確認範囲はaudit/store-chrome-reference.md、素材はaudit/store-chrome-assets.json。会員・マイカー・カートの状態を擬似表示せず、現行ショップへ接続する。

- 2026-09-16の画像指定『もともとこういう感じだったから、これも入れてほしい、いまのデザイン風に』: 動画と検索は維持し、ヒーロー直下の件数行と絞り込みの間に縦長写真の横スクロールを追加。public/photo-story.css/js、id=inspiration。既存featuredIds12件の実写真・実メタデータと詳細ダイアログを使用。白・赤・Josefin Sans、交互の段差、SPは次写真が少し見える幅。自動送りはせず、前後ボタン・スワイプ・キーボードに対応。これは以前の『縦長注目写真へ戻さない』指定に対する明示の追加依頼であり、ヒーローを旧デザインへ戻す変更ではない。

- 続く『ここ押したら対応車種のギャラリーに』: photo-storyのカードは写真詳細でなく車種別一覧へのリンクに変更。該当maker/carだけで絞り、ブランド・色・キーワード等の以前の条件は引き継がない。カードに『この車種の写真を見る →』を表示。URLにmaker/carと#photoResultsを保存し、通常クリックは一覧へ直接スクロール、新規タブや直接URLでも同じ一覧を開く。元の写真詳細は検索結果から利用。

- 続く『この文言も最後に入れたがいいかも』: support-sectionの後、共通フッターの直前に『シートカバー装着写真ギャラリーについて』と4ブランドの説明を追加。ユーザー画像の説明を現在のトーンに整理し、現資料と一致しない3,571事例・319車種・国内最大級の表現は固定で転載しない。件数はapp.jsでcatalog全件数から自動表示（現在2,897件）。public/gallery-about.cssでPC2列/SP1列のブランド説明。

- 『このバーが気になる、おしゃれにして』: 写真レールの横スクロールバーを3pxの細いライン・ブランド赤のつまみ・写真余白に揃えた左右の余白へ変更。Chromium/WebKitの矢印ボタンと太いOS標準枠は非表示、非対応ブラウザーはthinと同色を指定。ネイティブのドラッグ操作は維持。変更はphoto-story.cssのみ。
